# 무엇을 알아냈고 무엇을 아직 모르는가

2026-08-27 하루치 작업의 기억이다. 코드에는 남지만 사람의 머릿속에서는 사라지는 것들 — 왜 그렇게
했는지, 무엇을 재어 보고 정했는지, 무엇이 틀렸는지 — 을 적는다.

한 줄로 줄이면 이렇다. **오늘은 자를 만든 날이다.** 그 전까지 이 저장소에는 "좋아졌나"에 답할
수단이 없었고, 그래서 세 번 잘못된 결론을 내고 세 번 취소했다.

---

## 1. 가장 중요한 것 — 이제 ms 로 말할 수 있다

### 대리 지표만 있던 때

후보마다 붙는 값은 `anchor_density`(들려서 자리를 잡은 낱말의 비율)와 `breath_gaps`(줄 경계가
숨 쉬는 자리에 떨어진 비율)다. 둘 다 **"맞나"를 묻지 않는다.** 밀도는 "몇 낱말이 들렸나"를,
숨 자리는 "경계가 조용한 데 있나"를 물을 뿐이다.

실측이 그 한계를 그대로 보여 준다.

| 밀도 | 실제 오차 | 곡 |
|---|---|---|
| 0.99 | 308ms | Dolly Parton – Jolene |
| 0.90 | **26ms** | Drake – Janice STFU |
| 0.46 | 96ms | 米津玄師 – IRIS OUT |
| 0.92 | 396ms | Olivia Rodrigo – stupid song |

밀도 0.46 이 96ms 를 내고 0.99 가 308ms 를 낸다. **밀도를 1.0 으로 밀어붙이는 것은 과녁이
아니다** — 앵커 없는 낱말도 앞뒤 사이를 음절 무게로 나눈 시각을 받고, 앵커가 촘촘하면 그 값도
충분히 정확하다.

낱말 단위 79 곡 전부로 다시 재도 같은 말이 나온다.

| 밀도 | 곡 | 시작 오차 가운뎃값 | 0.1 초 안 |
|---|---|---|---|
| 0.70 미만 | 5 | **147ms** | 42% |
| 0.70~0.85 | 14 | 49ms | 67% |
| 0.85~0.95 | 21 | 47ms | 73% |
| 0.95~1.00 | 36 | **43ms** | 75% |
| 1.00 | 3 | 48ms | 72% |

**0.85 를 넘으면 그 위로는 평평하다.** 0.95~1.00 이 43ms, 딱 1.00 이 48ms — 1.0 이 오히려
조금 나쁘다(곡이 셋이라 우연일 수 있다). 전체 상관은 `r = -0.62` 인데, 그 상관은 거의 전부
**0.70 미만 다섯 곡**이 만든다. 밀도는 **바닥을 지키는 자**이지 **끌어올릴 과녁이 아니다.**
0.85 를 못 넘는 후보를 거르는 데 쓰고, 0.9 를 0.95 로 올리는 데 시간을 쓰지 않는다.

### 줄 단위 정답 — LRCLIB

`Generator/eval/truth.py` 가 LRCLIB 에서 받아 온다. 무인증 공개 API.

- `songs.json` 82 곡 중 **41 곡**에 싱크가 있다 (한국어 40 곡 중 18, 영어 32 곡 중 29)
  — 다만 한국어 18 곡 중 **13 곡이 로마자 표기**라 정답으로 쓸 수 있는 것은 **다섯 곡**이다.
  아래 언어별 표와 §10 이 쓰는 숫자가 그 다섯이다.
- **낱말 단위는 0 곡** — LRCLIB 에는 없다
- 2026-08-28 실측(41 곡 전부, 줄별 기록 포함): **오차 가운뎃값 147ms · 0.3 초 안에 든 줄 66%**
- 가장 좋은 곡 26ms/97%, 가장 나쁜 곡 7174ms/5%
- 대시보드에 올라가 있다 — 판 `53d1bcff`, 줄 1,863 개. 곡을 펼치면 줄마다 우리 시각·정답
  시각·차이가 나온다(`/admin/api/eval/<video_id>/lines`).

### 낱말 단위 정답 — JamendoLyrics

`Generator/eval/words.py`. `github.com/f90/jamendolyrics` 를 clone 하면 오디오까지 함께 온다.

- 79 곡 **21,580 낱말**, 낱말마다 시작·끝, CC 라이선스, mp3 390 MB
- 언어는 영어 20·독일어 20·스페인어 20·프랑스어 19. **한국어 없음**
- 우리 토크나이저가 가른 낱말 수가 정답과 **12 곡에서 모두 정확히 일치** → 번호로 1:1 견줌 성립
- 2026-08-28 실측(**79 곡 전부, 21,580 낱말**): **시작 오차 가운뎃값 48ms · 0.1 초 안에 든 낱말 73%**
- 온전한 기록이 남은 32 곡에서만: 시작 p90 **257ms**, 끝 오차 **75ms**, 0.3 초 안 **92%**
  (나머지 47 곡은 기계가 사라진 뒤 로그에서 되살린 것이라 이 셋이 없다 — §9 참고)

이 자로 알 수 있는 것은 "정렬기가 낱말을 제자리에 놓는가"이지 **"한국 노래에서 잘 되는가"가
아니다.** 뒤엣것은 손으로 맞춘 한국어 정답이 있어야 답할 수 있다.

### 자의 한계

LRC 는 사람이 손으로 찍은 것이라 그 자체가 ±수백 ms 흔들린다. 26ms 를 절대 정확도로 읽으면
안 된다. 믿을 수 있는 것은 **곡 사이의 견줌**과 **판 사이의 변화**다.

---

## 2. 오늘 세 번 속았다 — 전부 내 자가 고장나 있었다

같은 실수를 세 번 했고, 세 번 다 "발견"으로 보고했다가 취소했다. 다음 사람이 같은 자리를 밟지
않도록 적어 둔다.

| # | 무엇이 틀렸나 | 무엇이라 결론냈었나 | 진실 |
|---|---|---|---|
| 1 | LRCLIB 한국어 8 곡이 **로마자 표기**였다 | "아이돌·랩을 받아쓰기가 못 알아듣는다" | 로마자 가사를 먹인 것 |
| 2 | measure.py 가 `line.split()` 으로 낱말을 갈랐다 | "일본어가 완전히 실패한다" | 일본어 한 줄 = 낱말 하나가 되어 앵커가 붙을 자리가 없었다 |
| 3 | 받아쓰기 언어를 `"ko"` 로 못박아 두었다 | "받아쓰기가 병목이다" | 일본어 곡에 한국어로 받아쓰라고 시킨 것 |

`WOODZ - Drowning` 은 한글 0 자에 라틴 931 자다. 그 시트로 재고 "랩이 안 된다"고 읽었다.

셋을 고치고 다시 재니 일본어가 이렇게 바뀌었다.

```
米津玄師 IRIS OUT    밀도 0.00 → 0.50 ·  4800ms → 141ms
髭男dism Pretender  밀도 0.07 → 0.83 ·  1266ms → 174ms
髭男dism 115万キロ   밀도 0.35 → 0.78 ·  5464ms → 914ms
```

**정렬 코드는 한 줄도 안 고쳤다.** 언어와 토큰화만 바로잡았다.

`truth.py` 가 이제 로마자 시트를 걸러내고, `measure.py` 가 워커와 같은 `tokenizeV2` 를 부르며
(`tokenize.mjs`), 언어는 `sung_language` 로 보정한다. 표에만 적어 두면 다음에 또 밟는다.

---

## 3. 고친 버그

### 일본어가 영어로 넘어가던 것 (`written_language`)

분모가 `한글 + 라틴` 뿐이라 일본어 가사는 한글이 0 이고, 그래서 **라틴 비율이 언제나 100%** 였다.
라틴 스무 자만 섞이면 어떤 일본어 곡이든 영어 음소 모델로 갔다. 「IRIS OUT」이 그렇게 밀도 0.00,
오차 4.8 초를 받았다. 「Pretender」와 「115万キロ」가 멀쩡했던 것은 라틴이 3 자·8 자라 `total < 20`
에 걸려 **우연히** 빠져나간 덕이다.

이제 가나·한자를 분모에 넣는다. `ja` 후보는 **가나가 있을 때만** 내놓는다 — 한자만 있는 글은
중국어일 수 있고 한국어 가사에도 드물게 섞인다.

### 일본 곡이 한국어로 등록되던 것 (`inferLyricsLanguage`)

한글을 **먼저, 2% 문턱으로** 검사했다. 그 문턱 자체는 옳다 — Genie 머리글의 한글 네 글자가
Drake 를 한국어로 보내던 것을 막는 값이다. 깨지는 것은 "한글이냐 가나냐" 쪽이다. **멜론·벅스·FLO
는 일본 곡에 줄마다 한글 발음을 달아 준다.** 그러니 일본어 시트에 한글이 섞이는 것이 정상인데,
먼저 검사한다는 이유만으로 한국어가 됐다. 이제 한글과 가나가 함께 있으면 많은 쪽이 이긴다.

### 받아쓰기 언어를 그대로 믿던 것 (`expected_language` → `sung_language`)

`recording.language` 를 무조건 믿었다. 이제 가사 글자로 한 번 더 확인한다.

**여기서 `written_language` 를 그대로 쓰면 안 된다.** 묻는 질문이 다르다 — 정렬기는 *적힌 글자*가
필요하고 받아쓰기는 *부른 언어*가 필요한데, 로마자에서 갈린다. 로마자로 적힌 한국어 가사를 보고
"영어로 받아쓰라" 하면 곡을 통째로 잃는다. 그래서 `sung_language` 는 **한글·가나로만 뒤집고
라틴으로는 안 뒤집는다.** 한글로 적힌 가사가 영어 노래일 수는 없다.

### 품질 게이트에 `breath_gaps` 를 더함

줄과 줄 사이가 양옆보다 8 dB 이상 내려앉는 비율. 절대 문턱으로는 안 됐다 — `-50 dB` 로 재니
전주(1%)와 후주(5%)는 갈라냈지만 줄 사이 스물여섯 개가 모두 "노래 중"으로 나왔다. 스템에는
잔향과 숨이 남아 그 정도는 넘긴다.

문턱 0.65 는 열두 곡을 재어 놓았다. 열한 곡이 81~100% 에 모였고 한 곡만 40~50% 를 오갔다 —
관측값 위에 문턱을 두면 돌린 날에 따라 결과가 달라지므로 빈 구간 한가운데에 두었다.

**이 잣대는 비례로 나눈 짐작을 막지 못한다.** 그런 후보는 줄이 빈틈없이 붙어 있어 잴 틈이 아예
없고, 잴 것이 없으면 1.0 이 나온다. 그 길은 밀도 0 이 이미 막는다.

---

## 4. 해 보고 안 된 것 (다시 하지 말 것)

### 에너지 문턱으로 구절 끊기 — 실패

보컬 스템에 절대 dB 문턱을 두어 구절을 끊으려 했다. 곡 하나가 통째로 구절 **하나**로 나왔다.
demucs 스템에는 잔향과 새는 반주가 남아 줄 사이에 조용해지지 않는다.

### VAD 로 줄 경계 찾기 — 부분 실패

silero VAD 로 끊으니 **줄 수의 61~70%** 만 나왔다(병합을 전혀 안 해도 그렇다). 이유는 잣대가
아니라 음악이다 — **가사의 줄바꿈은 소리에 없는 사건이다.** 인쇄상의 약속이지 음악적 경계가
아니고, 가수는 두세 줄을 한 호흡에 붙여 부른다.

다만 61~70% 는 버릴 숫자가 아니다. VAD 경계는 받아쓰기가 틀려도 남으므로, **줄이 아니라 문장
단위 창**으로 쓰면 값어치가 있다. 아직 안 해 봤다.

### AGREE_SECONDS 조정 — 무의미

기존 세션에서 확인된 것. 값을 바꿔도 결과가 안 변한다.

### "merge-empty + monotone" 가설 — 20 곡에서 최고, 141 곡에서 최악

되돌렸다. 작은 표본에서 좋아 보이는 것을 믿지 말 것.

---

### 치우침(shift) 게이트 — 지을 것이 없었다 (2026-08-28)

「밀도 0.92 인데 7 초 틀린 곡」을 밀도가 못 잡으니 치우침으로 잡자고 했다. 네 갈래로 확인했고
**네 갈래 모두 지을 것이 없었다.**

1. **치우침은 정답이 있어야 나온다.** `ours - truth` 의 가운뎃값이다. 제품에는 truth 가 없다.
2. **꼬리넘침은 실재하지 않았다.** 곡 길이를 넘어 놓인 줄이 있는 줄 알았는데, 그 「길이」는
   LRCLIB 쪽 음원의 길이였다. **우리 음원 길이로 다시 재니 41 곡 중 0 곡**이다 —
   `align_variant` 의 `ceiling=duration_ms` 가 이미 막고 있다. 「back number - 水平線」의
   1.18 배는 우리가 386 초짜리 다른 업로드를 받았고 정답은 288 초 음원 기준이라 생긴 착시다.
3. **길이 불일치 검사는 이미 있다.** `Collector/src/youtube.ts` 의
   `RECORDING_DRIFT_TOLERANCE_MS = 3_000`, 실측으로 맞춘 값이다. 평가셋은 video_id 로
   **Collector 를 우회해** 받으므로 그 검사를 지나지 않는다 — 평가셋의 길이 불일치는
   제품의 실패가 아니다.
4. **나쁜 8 곡 중 4 곡은 지금 밀도 문턱이 이미 막는다.**

**남은 구멍은 4 곡이고 3 곡이 일본어다.** 이것이 이 조사의 유일한 소득이다.

### 죽은 가설 넷 (같은 날, 같은 자리)

전부 「곡 사이 상관」을 보고 세웠다가 「곡 안에서」 또는 「짜임을 갈라서」 다시 재니 사라졌다.

- **밀도를 1 로 올리면 정확해진다** → 79 곡에서 0.85 위로는 평평하다. 상관 `r=-0.62` 는
  거의 전부 0.70 미만 다섯 곡이 만든다. (§1)
- **곡 끝을 넘어간 줄이 있다** → 우리 음원 기준 0 곡. 위 2 번.
- **줄이 짧으면 꼬리가 터진다** → 곡 사이로는 뚜렷했다(40 자 이상 p90 429ms, 12~16 자
  3527ms). **한 곡 안에서 재니 짧은 줄 150ms, 긴 줄 135ms 로 차이가 없다.** 줄이 짧은
  곡이 마침 일본어였을 뿐이다.
- **되풀이되는 줄이 더 틀린다** → 전체로는 141ms 대 142ms 로 차이가 없다. **언어를 가르면
  일본어에서만 822ms 대 216ms (3.8 배)** 다. 영어·한국어는 차이가 없다.

**교훈: 곡 사이 상관은 언어와 얽혀 있다.** 무엇을 발견했다 싶으면 **한 곡 안에서** 다시
재고, **언어별로** 갈라 본다. 넷 중 셋이 그 자리에서 죽었다.

### 일본어가 네 배 나쁘다 — 원인은 되풀이 줄

| 언어 | 곡 | 가운뎃값 | p90 | 0.3 초 안 | 줄당 글자 |
|---|---|---|---|---|---|
| 영어 | 29 | 125ms | 778ms | 71% | 34.5 |
| 일본어 | 7 | **498ms** | **4932ms** | 48% | 13.4 |
| 한국어 | 5 | 181ms | 1439ms | 65% | 12.5 |

일본어는 **가운뎃값이 아니라 꼬리가 터진다.** 「米津玄師 - IRIS OUT」은 가운뎃값 124ms 인데
p90 이 3250ms 다. 되풀이되는 줄에서 회차를 잘못 잡는다 — 「back number - HAPPY BIRTHDAY」는
#10 과 #28, #11 과 #29, #30 과 #31 의 글이 같고 그 줄들이 4~7 초씩 틀린다.

**한국어는 이 증상이 없다**(되풀이 162ms / 한번뿐 182ms). 줄당 글자는 일본어와 같은데도
그렇다 — 짧은 줄이 원인이 아니라는 또 하나의 증거다.


## 5. 인프라에서 배운 것 (값을 치르고 알아낸 것들)

### 빌린 기계 고르기 — 실측

네 번 갈아탔고 네 번 다 짐작으로 골라 네 번 다 틀렸다.

| 무엇 | 실측 |
|---|---|
| **VRAM 이 동시판수를 정한다** | 16 GB 에 whisper 두 벌 = OOM. 25 GB 면 두 판이 선다 |
| **코어 수는 거의 상관없다** | demucs 를 스레드 4·8·16·31·64·128 로 돌려 **전부 29 초** |
| **GPU 등급은 값어치 없다** | A100 ×2($2.83/h) 가 A4000 ×1($0.15/h)보다 **느렸다** |
| **지역이 유튜브를 정한다** | 미국 데이터센터 74/74 가 403. 일본·한국·태국은 그냥 됨 |
| **`nproc` 를 믿지 마라** | 128 을 보여 주고 cgroup 할당량은 31. PyTorch 가 그 128 로 스레드를 띄워 서로 밟는다 |

정리하면 **VRAM 24 GB 이상 · 코어 24 이상 · 아시아 · 시간당 $0.20 안팎**. A100 두 장에서 곡당
390~650 초였던 것이 $0.15 짜리 A4000 한 장에서 곡당 56 초였다.

### vast.ai

- **`image_uuid` 가 곧 컨테이너다.** vast.ai 가 컨테이너를 못 돌리는 게 아니라, vast.ai 자체가
  우리가 고른 이미지를 돌려 주는 서비스다. 못 하는 것은 중첩 도커뿐이고 그건 필요 없다.
- **ssh 모드는 이미지의 entrypoint 를 갈아 끼운다.** 문서 그대로:
  *"The SSH and Jupyter launch modes inject setup scripts into your existing Docker image, which
  means your image's original entrypoint is replaced."* 이미지의 `CMD` 만 믿으면 워커가 안 뜬다.
  `entrypoint` 모드는 CMD 를 살리지만 ssh 가 없어져 들어가 볼 수가 없다.
- **인스턴스를 멈추면 GPU 를 놓친다.** 다시 켜려 했더니 `resources_unavailable` 이었다. 디스크는
  남지만 그 기계는 남이 가져간다.
- **API 는 자주 물으면 429 를 준다.** 물러섰다 다시 물어야 한다.

### 설치

- **dpkg 락 경합.** 빌린 기계는 우리가 붙는 순간에도 제 프로비저닝으로 apt 를 돌린다. 다투면
  `build-essential` 이 **조용히** 안 깔리고, 한참 뒤 `diffq` 가 C 확장을 컴파일하려다 터진다.
  `apt-get -o DPkg::Lock::Timeout=900` 으로 기다리고, gcc 가 없으면 그 자리에서 멈춘다.
- **Admin 을 빼고 세운다.** 두면 pnpm 이 react·vite·playwright·workerd 까지 받는다. 느린 회선에서
  몇 분이 나가고 npm 이 자꾸 되물린다. `test` 도 Admin 을 참조하므로 함께 뺀다.
- **9 GB 이미지보다 `curl | bash` 가 낫다.** 필요한 것만 받고, 언제나 그 순간의 `main` 을 세우므로
  이미지가 낡아 옛 코드로 도는 일이 없다.

### 인증

- **등록표(enrollment token)는 한 번 쓰면 끝난다.** 오래 사는 키를 기계마다 심지 않는 이유는,
  빌린 기계의 환경변수를 **그 기계 주인이 읽을 수 있고** vast.ai API 응답에도 그대로 나오기
  때문이다. 다 쓴 표는 주워도 쓸 데가 없다.
- **표의 만료가 십 분이면 모자란다.** 빈 기계가 스스로 세우는 데 그보다 오래 걸려, 네 대가
  도착하기도 전에 표가 죽었다. 이제 부르는 쪽이 시간을 말한다(최대 두 시간).
- **ghcr 는 fine-grained 토큰을 안 받는다.** 문서 그대로: *"GitHub Packages only supports
  authentication using a personal access token (classic)."* classic 의 `read:packages` 는 그 계정의
  **모든** 패키지를 연다 — 좁힐 방법이 없다. 그래서 이 패키지는 공개로 둔다. 이미지에 든 것은
  이미 공개된 코드뿐이라 잃는 것이 없고, 대신 남의 기계에 계정 열쇠를 두지 않는다.
- **Cloudflare 가 `urllib` 의 기본 User-Agent 를 1010 으로 막는다.** 우리를 밝혀야 한다.

### 기타

- **`d1_migrations` 에 0010·0011 이 빠져 있었다.** 두 판은 DB 에 손으로 적용되어 스키마에는
  들어와 있었는데 기록만 없어서, 새 판을 올리려 하면 이미 있는 표를 다시 만들려다 죽었다.
  스키마를 확인하고 기록을 채웠다.
- **`worker.stop()` 은 ML 데몬을 곧바로 닫는다.** 곡을 잡고 있는 동안 끊으면 그 곡을 잃는다.
  그래서 워커가 `.mora-worker-N.busy` 에 상태를 적고, 감독 스크립트는 `idle` 을 기다린다.

---

## 6. 지금 상태

### 저장소

`main` 이 `3534fa7`. 오늘 커밋 20 개쯤. 주요한 것:

```
3534fa7  잰 숫자가 맞는지 사람이 줄을 보고 판단할 수 있게 한다
0ce84dd  낱말이 몇 ms 틀리는지 재는 자를 들인다
ec9f724  빈 기계가 스스로 붙기까지 걸리는 시간을 등록표가 견디게 한다
3daf09f  한 줄로 기계를 세우는 설치 스크립트를 둔다
9c8dde8  잰 오차를 대시보드가 들고 있게 한다
9645f79  일본어 곡이 한국어로 등록되어 한국어로 받아쓰이던 것을 막는다
8e4c54f  일본어 가사를 영어로 넘기던 것을 고치고, 평가셋의 로마자를 걸러낸다
854612a  진짜 시각과 견주는 자를 들인다
```

### 데이터

- **대시보드는 비어 있다** (곡 0 · 작업 0 · 후보 0). 인증·설정·서비스 키만 남겼다.
- `collector.daily_budget = 0`, `auto_promotion_enabled = false` — 아무것도 자동으로 안 돈다.
- 평가 결과 41 곡이 `eval_runs`/`eval_songs` 에 있다. 줄별 기록(`eval_lines`)은 **아직 비었다** —
  그 기능을 만들기 전에 잰 것이라 다시 재야 채워진다.

### 도는 것

- **Collector**: galbook(사용자 리눅스 기계, 한국 IP, Ubuntu 26.04)에서 도커 컨테이너로. 목표 0 이라
  대기 중. galbook 은 Intel Arc(Lunar Lake) 다. CTranslate2 가 CUDA·CPU 만 지원하는 것은 맞지만
  그것은 **whisper 쪽 얘기**이고, 여기서 「Generator 로는 못 쓴다」고 내린 결론은 틀렸다 —
  torch `2.13.0+xpu`·transformers·demucs 4.1.0 이 모두 그 Arc 에서 돈다(§10). 강제 정렬과
  보컬 분리는 이 기계에서 한다.
- **빌린 기계 9 대**, 시간당 약 $1.57. 낱말 평가를 나눠 돌리는 중(38/79).
- 예산 $130 중 약 $22 사용.

### 도구

| 어디 | 무엇 |
|---|---|
| `Generator/eval/truth.py` | LRCLIB 에서 줄 단위 정답 (로마자 자동 필터) |
| `Generator/eval/measure.py` | 줄 오차 측정 |
| `Generator/eval/words.py` | JamendoLyrics 로 낱말 오차 측정 |
| `Generator/eval/tokenize.mjs` | 워커와 같은 `tokenizeV2` 를 부르는 다리 |
| `Generator/eval/publish.py` | 잰 것을 대시보드로 |
| `Generator/eval/breath.py` | 한 곡의 줄 경계가 숨 자리인지 |
| `Generator/scripts/install.sh` | `curl \| bash` 로 기계 하나 세우기 |
| `Generator/scripts/run-worker.sh` | 워커를 지키고 새 판에 갈아타기 |
| `Generator/scripts/rent.py` | vast.ai 에서 빌리고 워커로 띄우기 |

---

## 7. 아직 모르는 것 / 다음에 할 것

### 곧바로

1. **낱말 평가 79 곡 마무리** → 전체 숫자를 대시보드에
2. **41 곡을 다시 재서 `eval_lines` 채우기** — 지금은 곡을 열어도 줄이 없다
3. **기계 정리** — 9 대가 놀고 있다

### 남은 의문

- **`Teddy Swims - Lose Control`**: 밀도 0.92 로 게이트를 통과하는데 오차 7174ms, 치우침 +7.2 초.
  정렬 실패가 아니라 **다른 음원을 받은 것**으로 보인다. 밀도가 못 잡는 유형이 하나 더 있다는 뜻.
- **한국어 낱말 정확도를 모른다.** JamendoLyrics 에 한국어가 없다. 손으로 맞추는 자리는
  Admin 편집기가 아니라 따로 만든 검수 도구다 — `Generator/eval/review/` (§10).
- **문장 단위 창** — VAD 가 줄은 못 주지만 문장은 준다(줄 수의 61~70%). 받아쓰기와 무관한 창을
  얻는 길인데 아직 안 해 봤다.
- **`place_backing_runs`** — 오래된 미해결 건.
- **300ms 창 붕괴** — 속도 필터가 한 원인을 잡았고, 보간 뭉침(65%)은 아직 안 봤다.

### 학습에 대해

여러 번 오간 이야기라 결론을 적어 둔다.

- **정렬 모델은 학습 못 한다.** 낱말 단위 정답이 우리 것으로는 없다. Whisper 출력으로 학습시키면
  Whisper 의 실수를 배운다. JamendoLyrics 는 79 곡뿐이고 한국어가 없다.
- **받아쓰기 파인튜닝은 된다.** 시각이 필요 없고 (오디오, 가사) 쌍만 있으면 된다. 그것은 Collector
  가 모을 수 있다. **다만 지금 83 곡 ≈ 5 시간이고, 파인튜닝은 보통 수백 시간을 본다.**
- 그러니 **곡을 먼저 모아야 하고, 그건 GPU 가 아니라 Collector 의 일이다.**

### 손대지 않은 것

- **가사 라이선스.** `sales@lyricfind.com` 이 근본적인 상업 장벽. LyricFind 의 단어 싱크를 가져오는
  것은 하지 않기로 했다 — 라이선스 상품이다.
- 38% 의 no-lyrics 스킵은 쿼리를 고쳐도 안 된다(서비스에 가사가 없음). サザンオールスターズ 혼자
  99 건 중 41 건.

---

## 8. 사용자가 정한 것들

- 파일 수정은 **Bash 가 아니라 Edit/Write** 로.
- **Mac 에서 계산 일을 돌리지 않는다.** 빌려서 한다.
- LyricFind 는 안 쓴다. 영어·일본어는 **LRCLIB**, **한국어는 네이버 바이브**가 먼저다 —
  같은 마흔 곡에서 LRCLIB 은 다섯 곡, 바이브는 쳐 본 여덟 곡 모두였다(§10).
- 목표는 밀도 1.0 이 아니라 **낱말이 제자리에 있는 것**. (밀도는 그 대리 지표일 뿐이고 상관이
  느슨하다는 것을 실측으로 보였다.)

---

## 9. 자동화가 실패한 자리 (2026-08-28 새벽)

평가를 나눠 돌리려고 짠 것들이 연달아 실패했다. 코드는 저장소에 남지만 **왜 실패했는지**는
안 적으면 사라진다. 공통점이 하나다 — **띄웠다고 말하고 확인하지 않았다.**

### 곡 이름의 괄호가 쉘을 깨뜨렸다

`job.sh` 에 곡 이름을 문자열로 끼워 넣었다. JamendoLyrics 의 파일명에는 괄호가 섞여 있어
`syntax error near unexpected token '('` 로 통째로 죽었다. **몇 시간을 그 상태로 두었고 나는
몰랐다.** 죽은 것을 몰랐던 것이 죽은 것보다 나쁘다.

이제 곡 목록을 파일로 넘긴다 — `words.py --songs-file`, `measure.py @파일`. 이름을 쉘 문자열에
끼워 넣지 않으면 이 부류가 통째로 사라진다.

### 진행 계수가 틀렸다

`ssh ... 'grep -c "✔" 로그'` 로 셌는데 SSH 를 거치며 유니코드 매칭이 깨져 언제나 0 이 나왔다.
실제로는 돌고 있는데 "0/36" 으로 읽고 헛기다렸다. 원격에서 파이썬으로 세도록 바꿨다.

**교훈: 원격에서 문자를 세지 말 것.** 셀 것이 있으면 그쪽에서 파이썬으로 세고 숫자만 받는다.

### ssh 뒤로 백그라운드를 걸면 조용히 실패한다

`ssh ... 'cmd & disown'` 이 여러 번 아무것도 안 띄우고 성공한 것처럼 돌아왔다. `& sleep 1;
exit 0` 도, `setsid nohup` 도 그랬다. 확인 없이 "띄웠습니다" 라고 말한 것이 여러 번이다.

**지금 쓰는 방식:** 원격에 스크립트를 두고 `setsid nohup`, 그다음 **곧바로 `pgrep` 으로 PID 를
확인**한다. PID 가 없으면 안 뜬 것이다. 로그 파일 존재도 같이 본다.

### 배분기가 한 대에만 몰아줬다

tmux 세션 이름으로 "바쁨" 을 판정했는데, 시작하자마자 죽으면 세션이 사라져 다시 빈 기계로
보였다. 같은 기계에 여덟 덩어리를 쌓고 나머지 두 대는 한 번도 안 썼다.

**교훈: 상태를 물어 짐작하지 말고, 준 것을 준 것으로 기억할 것.**

### 두 대를 기다리다 한 대가 놀았다

"2 대 이상 준비되면 시작" 으로 짜서, 먼저 선 기계가 12 분을 놀았다. 기계마다 설치가 끝나는
때가 다르다. **한 대라도 서면 시작하고, 뒤늦게 선 기계는 합류시킨다.**

### 네트워크 오류 한 번에 거두는 쪽이 죽었다

vast.ai API 호출에서 `429` 만 재시도하고 `URLError` 는 안 다뤘다. 연결이 한 번 끊기자 루프가
죽었고, 기계 위의 작업은 계속 도는데 거두는 쪽만 사라졌다. **무엇이든 물러섰다 다시 묻는다.**

### 한 곡의 실패가 판 전체를 끝냈다

demucs 가 30 분 제한에 걸리자 그 자리에서 죽었고 그 기계가 맡은 열두 곡을 다 잃었다. 이제
곡 하나를 재는 일이 함수로 떨어져 있고, 터지면 그 곡만 건너뛴다. 제한도 5400 초로 늘렸다 —
느린 기계에서는 한 곡에 삼십 분을 넘긴다.

### 결과를 끝에 한 번만 거뒀다

빌린 기계는 언제든 사라진다 — 환불이든 자원 회수든 우리가 정하는 일이 아니다. 실제로 한 판을
그렇게 잃었고, 그 앞 판은 파기 직전에 로그를 긁어 겨우 복구했다. 이제 평가 쪽이 **곡마다**
`*-partial.json` 을 쓴다(84e4726).

### DNS 가 죽은 기계가 있었다

`mora-w35376519` 는 `github.com` 도 `youtube.com` 도 못 풀었다. `/etc/resolv.conf` 에 공개 DNS 를
넣어도 안 됐다 — 네트워크가 근본적으로 막힌 기계다. 파기하는 것이 답이다.

**빌린 기계는 멀쩡하다고 가정하지 말 것.** 세우기 전에 이름 풀기부터 확인하면 몇십 분을 아낀다.

### 이미지에 대한 판단이 거꾸로였다

"9 GB 를 받느니 세우는 편이 낫다" 고 보고 `install.sh` 로 돌렸는데, 받는 데 1~2 분이고 세우는
데 10 분이다. 네 번 갈아타며 그 10 분을 네 번 냈다. `rent.py` 는 다시 이미지를 쓴다(90ade88).
`install.sh` 는 **이미 켜 둔 기계**를 워커로 만들 때 쓴다.

### `pkill -f` 가 제 ssh 셸을 죽였다 (2026-08-28 낮)

`ssh ... 'pkill -9 -f words.py; echo 남은=...'` 을 세 번 보냈고 **세 번 다 출력이 비어서
돌아왔다.** 원격 셸의 명령줄에 `words.py` 가 들어 있어 `pkill` 이 **자기를 띄운 셸을 먼저
죽인** 것이다. 죽었는지 살았는지도 모른 채 "정리했습니다" 라고 말할 뻔했다.

괄호 요령(`w[o]rds\.py`)이 통하는 것은 **패턴 자체**뿐이다. 같은 명령줄 안에 죽일 이름이
문자 그대로 또 들어 있으면(예: `python -c "... get_model ..."` 과 `pkill -f g[e]t_model`)
여전히 자기를 잡는다. 그때는 `ps -eo pid,args | awk '/무엇/ && !/awk/ {print $1}'` 로
**PID 를 골라 `kill`** 한다.

**빈 출력은 성공이 아니다.** 출력이 없으면 명령이 끝까지 간 것이 아니라고 봐야 한다.

### 같은 내려받기를 여섯이 동시에 하다 전부 멈췄다

`mora-w33869585` 에서 낱말 평가가 한 시간 반 동안 한 곡도 못 냈다. 들어가 보니 demucs 가
여섯 개, 전부 CPU 1%, GPU 3 MiB, 모델 캐시는 0 개. 가장 오래된 것은 `futex_wait_queue_me`
였다 — **같은 가중치 파일을 동시에 받으려다 서로 잠금을 물었다.** 내가 재시작을 세 번 했고
앞의 것이 안 죽은 채 쌓였다(위의 `pkill` 문제 때문이다).

그 기계는 `curl` 로는 0.5 MB/s 로 받혔는데 `torch.hub` 는 10 분을 돌고도 **한 바이트도 쓰지
않았다.** 원인을 더 캐지 않고 파기했다 — 시간당 $0.296 을 쓰며 두 시간 동안 아무것도 만들지
않은 기계다. 남은 일은 멀쩡한 한 대로 옮겨 오히려 더 빨리 끝났다.

**교훈 둘.** 하나, 재시작하기 전에 앞의 것이 정말 죽었는지 센다. 둘, **못 쓰는 기계는 빨리
버린다** — 고치는 값이 다시 빌리는 값보다 크다.

### 로그에서 되살린 이름이 잘려 곡 수를 부풀렸다

기계가 사라진 뒤 로그를 긁어 되살린 55 곡의 이름이 로그 칸 너비에 맞춰 **34 자에서 잘려**
있었다. 잘린 이름은 온전한 이름과 다른 열쇠가 되어, 다시 잰 여덟 곡이 겹쳐 들어왔다.
`71 곡` 을 **`79 곡` 으로 읽고 "전곡 완료" 라고 말할 뻔했다.** 밀도 0.66 짜리 곡이 위아래로
두 번 나오는 것을 보고 알아챘다.

`merge.py` 가 정답 목록(`JamendoLyrics.csv`)의 이름에 앞자리를 맞춰 되돌린 뒤에 합친다.
**되살린 데이터는 온전한 데이터와 같은 열쇠를 갖지 않는다** — 합치기 전에 맞춘다.

### 짜임이 둘인 것을 모르고 평균을 냈다

되살린 55 곡은 로그 줄에 찍힌 것만 있어 `err`(정수 ms)·`within100`(백분율) 뿐이고, 새로 잰
24 곡은 `start_median_ms`·`start_p90_ms`·`end_median_ms`·`language` 를 갖는다. 없는 열쇠는
`.get()` 이 조용히 걸러내서, **24 곡으로 낸 p90 을 79 곡의 것처럼 찍었다.**

`merge.py` 가 `full` 로 어느 쪽인지 표시한다. **p90·끝오차·언어는 온전한 기록에서만 말한다.**

---

## 10. 한국어 정답을 만드는 길 (2026-08-28 낮~저녁)

§1 의 자는 영어 29 곡과 낱말 79 곡(한국어 0 곡)으로 세운 것이다. §7 에 "한국어 낱말 정확도를
모른다"고 적어 둔 그 구멍을 메우러 나선 날이다. 줄 단위 한국어 정답 **55 곡 2,382 줄**이
생겼고(`Generator/eval/korean-truth.json`), 낱말 정답을 사람이 두드려 만들 도구를 세웠다.

### 한국어에서는 LRCLIB 이 아니라 네이버 바이브다

`Generator/eval/songs.json` 의 한국어 40 곡으로 같은 자리를 두 출처에 물었다.

| | LRCLIB | 바이브 |
|---|---|---|
| 곡이 있나 | 40 중 **21** | 쳐 본 8 중 8 |
| 싱크가 있나 | 40 중 **18** | 8 중 8 |
| 한글 원문인가 | 18 중 **5** (13 곡이 로마자) | 8 중 **8** |
| 쓸 수 있는 것 | **5 곡 (12%)** | **8 곡 (8/8)** |

로마자가 왜 치명적인지는 §2 의 1 번에 이미 적혀 있다. **제품은 멜론·지니에서 한글 원문을
받는다.** 로마자 시트로 재면 파이프라인이 아니라 자료를 재는 것이 된다. LRCLIB 한국어에서는
그 함정이 예외가 아니라 다수다 — 싱크 있는 18 곡 중 13 곡이다.

바이브는 그 계열의 글자를 그대로 준다. 로마자 문제가 **구조적으로 없다.** 대신 줄의 끝 시각은
안 준다(`has_end: false`) — 그것은 아래 `lyricsfile` 이 채운다.

**두 출처를 함께 두는 값어치도 있다.** 같은 곡 「성시경 - 너의 모든 순간」 첫 줄이 LRCLIB
17600ms, 바이브 17770ms — **170ms 차이**다. 두 출처가 맞는 자리는 믿고 넘기고, 갈리는 자리만
사람이 듣는다.

### 제품 버그 — `vibe.ts` 가 없어진 구조를 읽고 있었다

`packages/songtitle/src/providers/vibe.ts` 가 `syncLyric.lyricLine[].startTimeMillis` 를 읽고
있었는데 **네이버가 그 구조를 바꿨다.** 지금 오는 것은 나란한 두 배열이다.

```
syncLyric: {
  startTimeIndex: [17.77, 31.27, ...],                       // 초 단위 실수
  contents: [{ languageType: "default", text: ["이윽고...", ...] }]
}
```

**`hasSyncLyric` 은 여전히 `true` 로 온다.** 깃발만 보아서는 알 수 없다는 뜻이고, 그래서
Collector 에서 바이브 싱크가 **조용히** 안 나오고 있었다. 쳐 본 여덟 곡 모두 새 모양이었고
두 배열의 길이도 모두 맞았다. 고쳤다 — 길이가 어긋나면 짧은 쪽까지만 쓴다.

또 하나. **바이브의 `playTime` 은 초가 아니라 `"04:03"` 꼴이다.** LRCLIB 의 `duration` 이 초
실수라 그쪽에 맞춰 `float()` 로 읽었다가 여섯 곡이 통째로 사라졌다(아래 「삼킨 예외」).

### LRCLIB 의 `lyricsfile` — 여태 안 보던 칸

응답에 `syncedLyrics`(LRC) 말고 `lyricsfile` 이라는 YAML 문자열이 있다. LRC 보다 낫다.

- **줄의 끝 시각(`end_ms`)까지 준다.** LRC 는 시작만 주므로 「시작은 맞고 끝이 틀린」 정렬을
  잴 수가 없었다. 200 항목 중 **176** 에 end_ms 가 있었다.
- 규격에 **낱말 단위(`words:`)도 있다.** 그러나 200 항목 중 **1 개**에만 있었고(NewJeans -
  Hurt), 그 하나조차 첫 줄만 차 있고 나머지는 `words: []` 였다. **아무도 안 채우는 규격이다.**

마지막 줄이 결론이다 — **한국어 낱말 정답을 긁어 올 데는 없다.** 손으로 만드는 수밖에 없고,
그래서 검수 도구를 지었다.

### LRCLIB 의 메타데이터는 올린 사람의 파일 태그다 — 검수 없이 저장된다

「지코」로 검색하면 `artistName="Hamah Music", trackName="지코 아무노래", albumName="지코
아무노래"` 짜리가 **1 위로** 온다. 올린 사람이 유튜브 채널 이름을 태그에 넣었고, `q=` 가
artistName·trackName·albumName **세 칸을 통째로 훑기** 때문에 앨범 칸에 걸려 올라온다.

`artist_name=` 만 주는 검색으로 좁히는 길은 **없다 — 0 건이 온다**(지원 안 함). 이 API 가 받는
조합은 `q=`, `track_name=`, `track_name=`+`artist_name=` 셋뿐이다.

**고치려 들 것이 아니라 아티스트 칸에 물어본 이름이 실제로 있는 행만 받으면 된다.** 같은 결과
안에 「지코(ZICO)」와 「지코」가 멀쩡히 함께 있다. 표기가 갈린 것은 속에 씨앗이 들어 있어
그대로 통과한다 — 「AKMU (악뮤)」, 「IU (아이유)」, 「Lim Young Woong (임영웅)」.

### 삼킨 예외가 몇 시간을 먹었다

바이브 검색이 빈 배열을 돌려주는데 원인을 못 찾았다. 서버 **밖에서** 프로브를 짜 같은 API 를
쳤더니 여섯 곡 다 성공했다. 다른 것은 하나뿐이었다 — 다른 쪽은 서버 **안**이었고, 거기
`except Exception: return None` 이 `ValueError: could not convert string to float: '04:03'` 을
삼키고 있었다.

**교훈: 삼킬 거면 자국은 남긴다.** 로그 한 줄(`print(..., file=sys.stderr)`)을 넣자마자 여섯
곡이 왜 사라졌는지 그 자리에서 나왔다. 한 곡이 넘어져도 나머지는 가야 하니 삼키는 것 자체는
맞다. 조용히 삼키는 것이 틀렸다.

### 한국어 정답 55 곡을 모았다

`Generator/eval/korean.py`(LRCLIB 에서 긁기) → `Generator/eval/resolve.py`(유튜브 video_id 붙이기)
두 단계다. video_id 는 음원을 받는 그 기계에서 붙인다 — 집 아이피로 수십 번 두드릴 일이 아니다.

- **묻는 방식이 수확률을 세 배 넘게 가른다.** 곡 이름을 하나씩 물으면 12%, **아티스트 이름으로
  물으면 46%**. 개별 조회에서는 로마자 시트가 주로 걸리고, 아티스트 목록에는 한글 원문이 많다.
- **씨앗을 다 돌지 않고 멈춘 판을 버렸다.** 앞쪽에서 60 곡을 채우고 멈췄더니 그 60 곡이 **발라드
  가수 여덟 명**의 것이었다. 아이돌도 힙합도 트로트도 없었다 — **오토튠과 랩이 정렬이 깨지는
  자리인데 그것이 통째로 빠진 표본이다.** 그 표본으로 낸 숫자는 실제보다 좋게 나온다. 이제
  씨앗마다 상한(3 곡)을 두고 78 개를 다 돈 뒤 아티스트를 돌아가며 뽑는다.
- 걸러야 하는 것들과 그 이유:
  - **반주·노래방 판**(`(Inst.)`·`MR`·`karaoke`) — 보컬이 없어 앵커가 하나도 안 붙는데, 그
    결과는 **「정렬이 나쁘다」로 읽히지 「노래가 없는 음원이다」로는 안 읽힌다.**
  - **트랙 번호 접두**(`602. 짠짜라`) — 앨범 목록에서 긁힌 것. 602 는 곡 이름이 아니다.
  - **아티스트·제목이 뒤바뀐 항목** — 그 이름으로 유튜브에 물으면 엉뚱한 곡이 온다.
  - **마지막 줄이 곡 길이를 넘는 항목** — 길이가 다른 판이거나 아예 다른 곡의 시트다.
    **67 곡 중 6 곡**이 그랬다.
- **유튜브 음원은 길이만으로 고르면 안 된다.** 길이 5 초 이내 **그리고** 제목·업로더에 아티스트나
  곡명 조각이 겹치는 것만 받는다. 길이만 보면 우연히 맞는 다른 곡이 통과하고, 그러면 **다른
  곡의 가사로 우리 정렬을 재게 된다** — 그 곡은 밀도 0 에 가깝게 나와 또 「정렬이 나쁘다」로
  읽힌다.
- 최종: 후보 60 곡 → 붙은 것 53 곡 → 반주 2 곡·길이 어긋남 2 곡 버리고 재시도로 4 곡 회수 =
  **55 곡 2,382 줄, 아티스트 55 명**(곡마다 다른 사람이다).

### 강제 정렬은 GPU 없이도 된다 — 그리고 Intel Arc 가 쓸 만하다

**받아쓰기는 필요 없다.** 무슨 말인지 이미 아니까 **아는 글자를 소리에 맞추기만** 하면 된다
(forced alignment). whisper 가 필요 없고 `torchaudio.functional.forced_align` 로 충분하다.

갤북(Intel Core Ultra 7 258V, NVIDIA 없음)에서 잰 것:

| 무엇 | 실측 |
|---|---|
| `torch.xpu.is_available()` | `intel-opencl-icd` + `libze-intel-gpu1` 깔고 torch `2.13.0+xpu` 로 갈면 **참**. 장치 이름 `Intel(R) Arc(TM) Graphics` |
| `forced_align` | **XPU 판이 없다.** 모델 추론만 XPU 에서 하고 맞추기는 CPU 로 내린다 |
| demucs 4.1.0 | 30 초 소리에 **4.5 초** (CPU 8.5 초) — **1.9 배**. 4 분 곡이면 0.6 분, 55 곡이면 **33 분** |

**§5 에 적힌 「CTranslate2 는 CUDA·CPU 만 지원한다」는 whisper 쪽 얘기다.** transformers/PyTorch 는
사정이 다르다. 그 한 줄 때문에 갤북의 Arc 를 **아예 안 보고 있었다** — 「Generator 로는 못
쓴다」가 「어떤 계산도 못 한다」로 굳었다.

정렬은 **곡을 통째로** 한다. 처음엔 줄마다 앞뒤 0.35 초 여유를 두고 잘라 맞췄다 — 「한 줄이
틀려도 옆줄로 안 번진다」는 생각이었는데, **거꾸로였다.** 창의 끝이 조금만 틀려도 남은 글자가
그 창 마지막 몇 프레임에 몰려 0.02 초씩 붙었다. 곡 전체를 한 줄의 토큰으로 놓고 맞추면 CTC 가
**전체가 가장 그럴듯한 길**을 고르므로 한 군데가 애매해도 뒤로 밀고 가지 않는다. 줄 시각은
이제 맞추는 데 안 쓰고 결과를 줄에 되돌려 주는 데만 쓴다.

### 모델을 갈아 끼웠다 — 한국어 ASR → MMS_FA

처음엔 한국어 ASR 모델(`kresnik/wav2vec2-large-xlsr-korean`)을 썼다. 어휘가 한글이라 로마자로
옮기는 단계가 없어 편했다. 그런데 그것은 **읽는 말**로 훈련된 것이고 우리가 넣는 것은 노래다.

`torchaudio.pipelines.MMS_FA` 는 애초에 **맞추기 위해** 만들어졌고 천 개 넘는 말을 함께 배웠다.
세 곡을 같은 자로 견줬다 — 줄 시작을 바이브 시각과 견준 오차, 곡 치우침은 뺐다.

| 곡 | 한국어 ASR | MMS_FA |
|---|---|---|
| 한로로 — 사랑하게 될거야 (28 줄) | 78ms · p90 1049ms · 0.3 초 초과 **6 줄** | 69ms · p90 895ms · **4 줄** |
| Ruru — 미안하다는말이제일어려워 (23 줄) | 15ms · p90 49ms · **1 줄** | 32ms · p90 63ms · **0 줄** |
| 릴러말즈 — Trip (62 줄) | 52ms · p90 746ms · **21 줄** · 61 줄만 맞춤 | 43ms · p90 **167ms** · **1 줄** · 62 줄 다 맞춤 |

**되풀이가 많은 랩 곡에서 갈린다.** 쉬운 발라드는 한국어 모델이 오히려 조금 낫지만(15 vs
32ms) 그 차이는 사람이 못 듣고, Trip 의 21 줄은 듣자마자 안다. 넓게 배운 모델이 분포 밖에서
덜 무너진다.

덤이 하나 더 있다. MMS_FA 의 어휘는 **로마자 스물아홉 자**뿐이라 한글을 `uroman` 으로 옮겨
넣어야 하는데, 그래서 **어휘 밖 글자가 아예 없어졌다.** 한국어 모델에서 「괜」은 1207 개 음절
어휘에 없어 통째로 못 붙었고(사용자가 되풀이 지적한 자리다) 그 어절 전체가 빈 채로 남았다.
로마자로는 `gwain` 다섯 자로 들어간다. **어휘를 좁게 맞추는 것보다 넓게 풀어 두는 편이 낫다.**

옮기기는 **글자마다 따로** 한다. 어절째 옮기면 로마자 낱자가 어느 음절에서 왔는지 잃는다.
음절 하나가 로마자 여러 자가 되니, 그 덩이의 **첫 자가 시작하는 때**가 그 글자의 시작이다.
로마자 첫 자는 대개 첫소리 자음이라 음절 경계와 잘 맞는다. 노랫말은 같은 글자가 숱하게
되풀이되므로 글자마다 한 번만 옮기고 적어 둔다.

바꾸며 걸린 것 둘 — `torchaudio` 모델은 `.logits` 가 아니라 `(확률, 길이)` **짝**을 돌려주고,
빈칸은 **0 번**이다(한국어 모델의 1204 번이 아니다).

**미심쩍음 표시는 새 모델에서 힘이 약하다.** 한국어 모델에서는 확신도가 둘로 갈렸다 —
맞은 자리는 정확히 0, 아닌 자리는 −3 아래. MMS_FA 는 가운뎃값이 −2 로 **이어져 있어** 그
칼이 안 든다. 문턱을 재 보니 −8.0 은 나쁜 줄 5 개 중 2 개만 잡고, 4 개를 잡으려면 −5.0 까지
내려야 하는데 그러면 113 줄 중 94 줄이 노래진다. **−8.0 으로 둔다**(글자의 3~4%). 잡는 힘이
약한 것을 알고 쓰는 것이지, 문턱을 고쳐 나아질 것이 아니다.

### 가장 값진 발견 — **바이브와 다른 것과 우리가 틀린 것은 다르다**

성능을 더 올리려다 자를 먼저 의심했다. 여덟 곡 442 줄로 재니 「밖에서 온 줄 시각과 0.3 초
넘게 어긋난 줄」이 **12%**(55 줄)였고 최악은 **14.6 초**였다. 세 곡만 볼 때의 4% 와 딴판이라
표본이 운이 좋았던 것이다.

그런데 그 55 줄이 정말 우리 실수인가. 크게 어긋난 열다섯 줄을 **소리로 대질**했다 — 같은
길이의 창 두 개(우리가 고른 자리 / 바이브가 말한 자리)에서 그 줄의 토큰만 각각 맞춰 보고
글자당 평균 점수를 견줬다(`probe_prior.py`).

| | 줄 | 보기 |
|---|---|---|
| **바이브 자리가 나음** | **1** | 사랑하게 될거야 26 번 (+2.07) |
| 창을 옮겨도 **같은 자리**를 고름 | 5 | 19·24 번, 파란달팽이 21 번 — 모델이 확신하는데 바이브가 다름 |
| **우리 자리가 나음** | 9 | 파란달팽이 47·48 번 (−2.75, −6.01), Small girl 60·61·62 번 (−2.08, −4.26, −5.86) |

**둘이 다를 때 우리가 옳은 쪽이 압도적이었다.** 유튜브 음원과 네이버 마스터는 편곡이 다르다
— 되풀이 횟수가 다르거나 간주가 다르면 곡이 통째로 어긋난다. 14.6 초짜리 「오차」는 우리
정렬이 막힌 것이 아니라 **판이 다른 것**이었다.

그러니 **「바이브와의 어긋남」을 성능 지표로 쓰면 안 된다.** 그 자로 최적화하면 맞는 것을
틀린 쪽으로 끌게 된다. 실제로 설계 후보 셋(직접 CTC 비터비에 줄머리 닻, 무음 못 박기,
시간 띠 벌점)을 따로 세워 심사했더니 셋 다 5/10 이었는데, 심사자들이 짚은 것이 정확히
이것이었다 — **바이브 쪽으로 무조건 끌면 곡 6·7 을 14 초씩 망가뜨린다.**

### 2 차 되짚기 — 소리에게 물어서만 옮긴다

되풀이되는 구절에서 정렬이 엉뚱한 자리를 붙잡는 것은 **진짜다.** 「그럼에도 불구하고」가
곡에 세 번 나오는데 26 번만 2.66 초 앞으로 끌려가 5.30 초에 걸쳐 늘어졌다(다른 두 번은
2.56·2.58 초). 앞뒤 줄(25·27)은 다 맞았다.

고친 방식(`rethink` / `pinned`): 1 차로 곡 전체를 맞춘 뒤, 밖에서 온 시각과 `SUSPECT_MS`
넘게 어긋난 줄만 위의 대질에 부친다. 바이브 자리가 **`EVIDENCE`(1.0) 넘게 나을 때만** 못을
박고, 그 자리에서 정렬을 갈라 토막마다 다시 맞춘다. 한 토막이라도 실패하면 통째로 물러선다.

이것이 앞서 실패한 「어긋난 줄만 그 앞뒤 사이에서 다시 정렬」과 다른 점은 하나다 —
그건 **1 차가 고른 경계**로 창을 좁혀 같은 부분문제를 다시 푼 헛일이었고, 이건 **밖에서 온
시각**이라는 새 정보로 다른 창을 만들어 견준다.

문턱 1.0 은 임의의 값이 아니다. 위 표에서 우리가 틀린 무리(+2.07, +0.65, +0.42)와 바이브가
틀린 무리(−2.08 ~ −6.01)가 ±0.5 언저리에서 겹친다. 애매한 것을 옮기려다 곡 6·7 을 망가뜨리는
것보다 **고칠 수 있는 것만 고치는 편이 낫다.**

성적: 여덟 곡에서 **옮긴 줄 1 · 고친 줄 1 · 망가뜨린 줄 0 · 덩달아 움직인 줄 0**
(`probe_compare.py`, 문턱 0.5/1.0/2.0/3.0 훑음). 26 번이 −2660ms → +60ms 가 됐고 줄 길이도
5.30 초 → 2.58 초로 다른 두 번과 같아졌다.

**적게 고쳤다고 실패가 아니다.** 대질이 말해 주는 것은 고칠 것이 원래 그만큼이었다는 것이다.

### 무너진 줄을 밖의 자 없이 찾기 (`flag_stuck`)

사람이 화면에서 보는 「막힘」은 바이브로 못 찾는다. 그래서 **정렬 결과 안의 모순**만 본다:

1. **같은 글월이 곡에서 다른 길이로 놓임** — 가장 잘 든다. 「(If, if I got a…)」 가 한 번은
   0.88 초, 바로 다음은 10.23 초. 뒤엣것이 앞엣것 소리까지 먹은 것이다. 무너진 줄은 3~4 배,
   멀쩡한 되풀이는 1.3 배 안쪽이라 `STRETCH = 1.8` 로 가른다.
2. **글자가 죄다 최소 간격(`CRAMP_MS`)에 붙음** — 정렬이 더 좁히려던 것을 규칙이 막은 자국.
   속도로 재면 안 된다. 세 글자는 최소 간격만 지켜도 초당 18 자라 짧은 줄이 억울하게 걸린다.
3. **줄 안이 2 초 넘게 빔.**

442 줄 중 **24 줄(5%)** 을 짚는다. 멀쩡한 곡(미안하다는말, To. 난간)에는 0 개다.
글자마다의 확신도(`DOUBT`)를 대신하는 것이 아니라 **줄 단위로 따로** 붙는다 — 확신도는
MMS_FA 에서 힘이 약해(나쁜 줄 다섯 중 둘) 「어디부터 들을까」에 답을 못 했다.

### 목소리를 갈라야 풀리는 것 — 리드 / 서브 보컬

강제 정렬은 **차례를 지킨다.** 가사의 글자를 적힌 순서대로만 소리에 붙일 수 있다. 그런데
백보컬은 리드와 **같은 때에** 불린다. 가사에는 앞뒤로 적혀 있으니 정렬은 둘을 차례로 놓으려
하고, 그러다 한 줄이 다른 줄의 소리까지 먹는다 — Small girl 의 「(If, if I got a…)」 가
한 번은 0.88 초에 눌리고(초당 13.6 자) 다음 번은 10.23 초로 뻗었다. 두 구간이 겹쳤다.

**한 갈래 소리에 두 목소리가 섞여 있는 한 이건 못 고친다.** demucs 로는 안 된다 — 보컬이
한 갈래(`drums·bass·other·vocals`)라 리드와 백이 같이 나온다. UVR 계열 카라오케 모델이
그 둘을 가른다(`audio-separator` 의 `mel_band_roformer_karaoke_aufr33_viperx`).

**XPU 로 돌려야 쓸 수 있다.** audio-separator 는 cuda·mps·directml 만 보고 Arc 를 못 찾아
CPU 로 떨어지는데 4 분 곡에 **20 분 51 초**가 걸린다. 만든 뒤 `separator.torch_device` 를
`xpu` 로 갈아 끼우면 **약 4 분** — 5.4 배다. `batch_size` 는 CPU 에서 아무 효과가 없었다.

#### 여기서 두 번 크게 헛디뎠다 — 둘 다 값진 실패다

**1. 갈래마다 제 줄만 남기고 따로 맞췄더니 무너진 줄이 2 개 → 20 개.**
줄을 빼면 그 차례에 **구멍**이 나고, 남은 줄이 그것을 메우려 뻗는다. 백보컬이 거의 없는
곡(파란달팽이)에서는 얻을 것도 없이 잃기만 했다. **차례에서 줄을 빼는 것은 공짜가 아니다.**

**2. 줄은 다 두되 바탕을 리드 갈래로 바꿨더니, 한 줄도 구제 안 했는데 2 개 → 6 개.**
카라오케 모델이 백보컬을 걷으면서 **리드도 깎는다.** 맞추기에는 demucs 보컬이 더 낫다.
가른 것 중 쓸모 있는 것은 **서브 쪽뿐**이었다 — 리드 갈래는 만들되 쓰지 않는다.

#### 되는 설계 — 바탕은 그대로 두고 무너진 줄만 구제한다

1. demucs 보컬 위에서 **모든 줄을** 맞춘다. **아무것도 바꾸지 않는다.**
2. 무너진 줄(`flag_stuck` + 앞줄과 겹침)만 고른다.
3. 서브 갈래에서 **줄을 다 두고** 한 번 더 맞춘다.
4. 무너진 줄에 한해, 서브 쪽이 `VOICE_EDGE` 넘게 낫고 리드가 짚은 자리에서
   `RESCUE_REACH_MS` 안이면 그 시각을 쓰고 `lane = 1` 로 표시한다.

멀쩡한 줄은 손대지 않으므로 **나빠질 자리가 없다.** 3 번에서 무너진 줄만 남겨 봤다가 또
헛디뎠다 — 네 줄만 두면 곡 어디에나 갈 수 있어 **47~147 초씩 튀었다.** 「(If, if I got a…)」
는 곡에 열 번 넘게 나오니 어느 것에 붙어도 점수가 좋다. **줄을 다 둬야 차례가 묶어 준다.**

| 곡 | 한 갈래 | 두 갈래 | 구제 |
|---|---|---|---|
| 5 너와 나 · 7 Small girl | 4 | **2** | 2 |
| 6 파란달팽이 | 2 | **1** | 1 |
| 1·2·3·4·8 | 그대로 | 그대로 | 0 |

**넷이 좋아지고 넷은 그대로, 나빠진 곡은 없다.**

한 가지 더 — 합친 뒤에 **`flag_stuck` 을 다시 매겨야 한다.** 그것은 `align_song` 안에서
도니, 줄을 갈아 끼우면 표시가 옛 결과를 가리킨 채 남는다.

#### 파생 파일이 원본 자리를 빼앗았다 — 가장 비싼 실수

서버의 `audio_path` 는 `sorted(AUDIO.glob(f"{video_id}.*"))` 의 첫 번째를 원본으로 썼다.
보컬을 가르며 `{video_id}.back.wav` 가 생기자 그것이 `.m4a` 보다 **사전순으로 앞서서**,
사람이 듣는 재생도 정렬의 바탕도 통째로 **서브 보컬만 남은 소리**가 됐다.

사용자가 「재생되는 음원이 이상하다 · 사람 목소리가 굉장히 기계음으로 나온다」고 했다.
그 말이 정확했다 — 백보컬만 남은 찌꺼기를 듣고 있었던 것이다.

probe 들은 이 거르기를 제 안에 갖고 있어 멀쩡했고 **서버만 틀렸다.** 그래서 같은 곡을
재는데 값이 달랐고, 나는 그 차이를 `flag_stuck` 탓으로만 읽고 한참 헤맸다. 그것도 진짜
버그였지만 **더 큰 것이 뒤에 숨어 있었다.**

교훈 둘.

1. **만들어 낸 파일을 원본 옆에 같은 이름으로 두면, 원본을 고르는 코드가 반드시 그것을
   집는 날이 온다.** 걸러 내는 자리를 한 곳(`MADE_FROM`)에 두고 거기서만 판단한다.
2. **재는 곳과 쓰는 곳이 다른 길로 파일을 고르면, 값이 갈릴 때 원인을 못 찾는다.**
   probe 가 옳고 서버가 틀렸는데, 둘이 다르다는 사실 자체가 단서였다.

겹침을 이제 결함으로 세면 안 된다. 백보컬은 리드와 **실제로** 겹치고, 그것을 그리려고
화면에 레인을 만든 것이다.

### 누가 부르는가 — 화자 가르기

카라오케 모델이 가르는 것은 **리드 대 화음**이지 **누구냐**가 아니다. 피처링 가수는 저 혼자
리드로 부르므로 리드 갈래에 그대로 들어가고, 목소리가 확 바뀌어도 안 갈라진다. 갈래 에너지
비로 재 보니 정답 6 줄 중 1 줄만 잡히고 헛잡음이 2 였다 — **화음 문제가 아니었다.**

목소리 자국(`speechbrain` 의 ECAPA-TDNN)을 **줄 단위로** 떠서 묶는다. 소리를 창으로 훑어
화자 경계를 찾는 흔한 방식을 안 쓰는 이유는 **우리가 이미 줄 경계를 알기** 때문이다.

#### 「사람이 몇인가」에서 세 번 헤맸다

1. **거리 문턱** — 0.25~0.55 를 훑으니 묶음이 15~47 개로 흩어졌다. ECAPA 는 **말**로 훈련된
   것이라 노래에서는 같은 사람의 자국도 음높이·창법에 따라 크게 흔들린다.
2. **실루엣 점수** — **솔로 곡이 피처링 곡보다 높게 나왔다**(0.350 대 0.322). 묶임새는 사람이
   갈리는지가 아니라 **창법이 갈리는지**를 잰다. 이게 가장 값진 실패다.
3. **제목의 `Feat.`** — 잘 되긴 하는데 **그룹을 통째로 놓친다.** 빅뱅 같은 팀은 넷다섯이
   번갈아 부르는데 제목에는 아무 표시가 없다. 사용자가 짚어 줬다.

#### 되는 자는 **덩어리짐**이다

사람이 바뀌면 그 사람이 한 절을 통째로 부르므로 줄 번호가 **이어진 덩어리**로 나온다.
한 사람의 창법 변화는 곡 여기저기에 흩어진다. **그 차이는 묶임새가 아니라 자리에서 드러난다.**

| | 다른 사람이 있는 곡 | 혼자인 곡 |
|---|---|---|
| 가장 긴 이어진 덩어리 | **7 · 12 · 11** | 4 · 1 · 5 · 1 · 2 |

`VOICE_RUN = 6` 이면 앞의 셋을 다 잡고 뒤의 다섯을 하나도 안 잡는다. 제목에 `Feat.` 이
있으면 3 으로 낮춘다 — 없다고 솔로인 것은 아니지만, 있으면 확실하다.

성적 — 여덟 곡에서 솔로 다섯은 하나도 안 갈리고, 피처링 셋은 다 갈렸다. Small girl 은
사용자가 짚어 준 정답 6 줄 중 **4 줄을 헛잡음 없이** 잡았다(20·62 번은 자국이 메인 가수처럼
읽힌다). 레인 나눔 `{0: 41, 1: 18, 2: 4}` — 이영지 41 줄, 도경수 18 줄, 백보컬 4 줄.

**아직 못 재 본 것** — 그룹 곡(빅뱅·방탄 같은)이 이 저장소에 없다. 멤버가 한두 줄씩 번갈아
부르면 덩어리가 짧아 이 자에 안 걸릴 수 있다. 그런 곡을 하나 넣어 봐야 안다.

### 앞 모델(한국어 ASR)에서 밟은 함정 넷

아래 1·2 는 그 모델에만 해당한다. 3·4 는 지금도 그대로다.

1. **어휘를 자모로 못박았다 → 31 줄 중 0 줄.** `kresnik/wav2vec2-large-xlsr-korean` 의 어휘는
   **음절**(1207 개, 「가」·「볍」·「칠」)이지 자모가 아니다. 음절 모델에 NFD 로 편 자모를
   내밀면 어휘에 하나도 없어 **맞출 글자가 아예 안 남는다.** 그대로(NFC) 대 보고 80% 이상
   맞으면 음절, 아니면 자모로 폈다. 이 판정을 **어절마다** 하다 또 한 번 넘어졌다 — 「괜찮아」는
   셋 중 둘만 어휘에 있어 80% 에 못 미쳐 자모로 폈고, 그러면 아무것도 안 남았다. 모델 하나에
   대해 **한 번만** 물을 일이었다. 로마자로 옮겨 넣는 지금은 이 판정 자체가 없다.
2. **빈 토큰(blank) 번호를 0 으로 잡았다.** `<pad>` 를 찾다 못 찾고 0 으로 물러섰는데, 이 모델의
   0 번은 글자 **「볍」**이고 `[PAD]` 는 **1204** 다. 엉뚱한 글자를 빈칸으로 치고 맞췄다.
   토크나이저에게 `pad_token_id` 를 직접 묻는다.
3. **프레임을 토큰으로 셌다.** 같은 토큰이 여러 프레임 이어지는데 매 프레임을 새 토큰으로 세어
   토큰 목록을 훨씬 빨리 소진했다. 증상은 **앞 낱말들이 0.06 초씩 뭉치고 마지막 낱말이 남은
   시간을 다 먹는 것**이었다. 묶는 일은 `torchaudio.functional.merge_tokens` 가 한다.
4. **반주가 섞인 원곡을 맞추고 있었다.** 꼬리를 자르려고 「모델이 빈칸으로 보는 프레임」을
   찾았는데 하나도 없었다 — **노래가 멎어도 모델은 반주를 듣고 아무 글자나 내놓지 빈칸을
   내놓지 않는다.** Mora 파이프라인은 demucs 로 보컬을 뽑은 뒤 맞추는데 이 도구에는 그 단계가
   빠져 있었다. `align.py` 가 이제 `htdemucs` 로 보컬을 먼저 뽑는다(`htdemucs_ft` 는 네 모델을
   겹쳐 돌려 네 배 걸리는데, 맞추기에 넣을 보컬에 그만한 값을 치를 이유가 없다).

반주가 섞인 채로 잰 결과 — 한로로 「사랑하게 될거야」 31 줄: **줄 28/31 · 낱말 95 · 낱말 길이
가운뎃값 0.84 초 · 2 초 초과 19 개.** 2 초 초과가 많은 것은 줄 끝 낱말이 다음 줄 직전까지
늘어나기 때문이고, **그것이 4 번의 증상이다** — 숫자를 정렬 품질로 읽으면 안 된다.

### 검수 도구 (`Generator/eval/review/`)

**msi 에서 돌고 Tailscale 안에서만 닿는다**(`http://msi.tail277268.ts.net:8787`).
`mora.junx.dev` 에는 올리지 않는다 — 인증이 없다. 갤북(`galbook.tail277268.ts.net:8787`)에도
같은 것이 그대로 살아 있다 — 물러설 자리로 둔다. `./deploy.sh galbook` 으로 그쪽에 올린다.

#### 갤북에서 msi 로 옮겼다

같은 60 초 조각, 같은 모델로 잰 값:

| | **msi** | 갤북 |
|---|---|---|
| GPU | **RTX 3060 Laptop 6GB**(CUDA) | Intel Arc 140V(XPU) |
| 메모리 | 14GB | 30GB |
| 보컬 가르기 | **25.0 초** (4 분 곡 100 초) | 58.0 초 (232 초) |
| 정렬 추론 | **0.82 초** (4 분 곡 3.3 초) | 2.10 초 (8.4 초) |

가르기 2.3 배, 추론 2.6 배 빠르다. 다만 **한 곡을 서버로 맞추는 시간은 둘 다 15 초로 같다** —
갈래가 캐시된 뒤에는 GPU 가 병목이 아니라 모델 올리기와 데이터베이스가 대부분이다.
**차이는 캐시 없는 첫 처리에서만 난다.**

옮기며 걸린 것들:

- msi 는 파이썬 3.14 뿐이라 torch 휠이 없다. `uv venv --python 3.12` 로 따로 깐다.
- **sudo 없이 깔았다.** node 는 공식 tarball 을 `~/.local/node` 에 풀고, ffmpeg 는
  `imageio-ffmpeg` 가 들고 오는 정적 바이너리를 `~/.local/bin/ffmpeg` 로 건다. 비대화형
  ssh 는 그 자리를 PATH 에 안 넣으므로 `deploy.sh` 가 `REMOTE_PATH` 로 넣어 준다.
- **`motion` 이 `package.json` 에 없었다.** 갤북에 손으로 깔고 저장을 안 한 것이다. 옮기고
  나서야 빌드가 깨져 알았다 — 기계를 옮기는 것은 「무엇이 손으로 깔렸나」를 드러낸다.
- 기계끼리 rsync 는 Tailscale SSH 인증이 없어 안 된다. `ssh a 'tar cf -' | ssh b 'tar xf -'`
  로 맥을 통과만 시킨다(디스크에 안 쓴다). 257MB 에 몇 분.
- `onnxruntime-gpu` 가 CUDA 13 라이브러리를 찾다 실패하는 경고를 낸다(torch 는 cu124).
  **Roformer 는 torch 로 돌아 실제 동작엔 지장이 없다** — 위 25.0 초가 그 상태로 잰 값이다.

- 스택: FastAPI + Vite/React + motion. 갤북에 `uv`, node 22, ffmpeg, torch(xpu), demucs 를
  깔았다. 실행은 `~/mora-review`.
- 하는 일: 바이브·LRCLIB 에서 가사 찾기, 유튜브에서 음원 찾기·받기, 제목·아티스트 고치기,
  곡 전체 치우침 보정, **줄 눈금을 끌어 그 줄만 옮기기**, 낱말 시각을 끌어다 놓기,
  맞음·밀림·틀림 판정, 정답 파일 내보내기.
- 긁어 온 55 곡을 **미리 채우지 않는다.** 밀어 넣으면 검수가 「이미 있는 것을 훑는 일」이 되는데,
  무엇을 넣을지 고르는 것부터가 검수다.

가사 화면은 Apple Music 을 흉내낸다. 만드는 법은 `~/Documents/Reprise` 의
`PlayerPopoverView.swift` 안 `InlineLyricsView` 를 옮겨 왔다. **핵심은 스크롤이 아니라는
것이다** — 줄마다 제자리가 있고, 지금 줄이 바뀌면 **모든 줄이 저마다 새 자리로** 스프링
(`duration 0.56, bounce 0.24`)으로 움직인다. 그리고 **지금 줄 아래의 줄들이 한 줄에 0.045 초씩
늦게 떠나는 것**(3 줄까지)이 「뭉치가 자리를 잡는」 느낌을 만든다. 위아래 가장자리는 마스크로
옅어지게 한다.

낱말은 `background-clip: text` 로 그러데이션을 글자 모양으로 오려 왼쪽에서 오른쪽으로 차오르게
한다. **여기에는 스프링을 걸지 않는다** — 걸면 소리보다 뒤처져 「안 맞는다」로 읽힌다.

### 이 도구를 쓸 때의 원칙 — 순환을 만들지 않는다

모델을 한 번 돌려 맞춰 놓고 사람이 고치는 것은 **출발점**이지 정답이 아니다. 이대로 두고
「맞음」을 누르면 **우리 모델의 실수가 그대로 정답이 되고, 그 정답으로 다시 우리 모델을 재게
된다.** 순환이다.

§4 에 「파이프라인 출력으로 정답을 만들면 안 된다」고 적어 둔 것과 경계가 흐리니 갈림길을
명시한다 — **사람이 실제로 듣고 고쳤는가.** 고치지 않고 넘긴 곡은 정답이 아니다.

### 이번에도 되풀이한 실수

- **`window` 라는 지역 변수를 만들어 전역 `window` 를 가렸다.** 바로 아래
  `window.addEventListener` 가 **배열에** 이벤트를 걸려 해서 키 입력이 통째로 안 걸렸다.
  타입 검사가 잡아 줬다.
- **HTTP 200 을 보고 「올라갔다」고 여겼다.** 서버는 살아 있어도 `dist` 가 옛것이면 화면은
  그대로다. 실제로 한 판 뒤처진 번들을 보며 「가라오케가 안 된다」를 한참 뒤졌다. **배포 확인은
  번들 안의 새 글자를 grep 하는 것으로 한다**(`grep -l 배속 dist/assets/*.js`). 어긋남을 가장
  확실하게 보는 법은 **맥과 갤북의 md5 를 나란히 찍는 것**이다.
- **시계를 둘 두었다.** 어느 줄을 켤지는 밖에서 온 **줄 시각**으로 정하면서, 글자를 칠하는
  것은 우리가 맞춘 **글자 시각**으로 하고 있었다. 둘은 가운뎃값 76ms, p90 1051ms, 최대 1.4 초
  어긋난다 — 줄이 켜지는 순간 첫 글자 두어 개가 **이미 다 차 있거나**(28 줄 중 3 줄),
  줄은 켜졌는데 최대 1.05 초 동안 아무것도 안 움직였다. 값은 멀쩡한데 화면이 딴 시계를
  보고 있었다. **화면이 보여 주는 것을 그대로 따라 켜야 한다** — `activeIndex` 가 그 줄의
  첫 맞춘 글자를 따르게 고쳤다(맞춘 게 없으면 줄 시각으로 물러섬). 옆에 적히는 시각만
  바이브 것 그대로 뒀다 — 그건 견줄 자이지 그릴 자가 아니다.
- **`pkill -f` 로 제 셸을 세 번 죽였다.** 원격 명령줄에 그 문자열이 들어 있어서다.
  `deploy.sh` 에는 고쳐 뒀는데 손으로 칠 때 또 밟았다. 패턴을 `"spee[d].py"` 처럼 써서
  제 명령줄이 안 걸리게 하거나, `pgrep` 으로 PID 를 얻어 `kill` 한다.
- **찍기 타임라인에 레인을 두었다.** 한 줄짜리로는 **겹치는 목소리를 그릴 수가 없다** —
  막대가 서로 포개져 어느 것이 어느 목소리인지 안 보인다. 영상 편집기처럼 목소리마다 칸을
  준다(`메인`/`서브`). 어느 레인인지는 서버가 `line.lane` 으로 준다 — 괄호로 짐작하지
  않는다. 442 줄 중 괄호 친 줄은 아홉뿐인데 백보컬은 그보다 훨씬 많다.
- **찍기 목록이 노래를 따라 굴러간다.** 따라야 하는 것은 **소리가 있는 줄**이지 찍는
  자리가 아니다 — 다 찍은 곡에서는 찍는 자리가 끝줄에 머물러 안 따라온다. 사람이 직접
  굴리면 2.5 초간 비켜 준다. `scrollIntoView` 는 안 쓴다(창 전체가 튄다).
- **`onTimeUpdate` 는 초당 네 번쯤만 온다.** 줄 단위엔 충분해도 **0.4 초짜리 낱말은 통째로
  건너뛴다.** 재생 중에는 `requestAnimationFrame` 으로 읽는다.
- **curl 로 한글이 든 URL 을 그냥 치면 uvicorn 이 요청 줄에서 거부한다**(400
  `Invalid HTTP request`). `--data-urlencode` 를 쓴다.

### 이 절에서 생긴 것

| 어디 | 무엇 |
|---|---|
| `Generator/eval/korean.py` | LRCLIB 을 아티스트 씨앗 78 개로 넓게 긁어 후보 만들기 |
| `Generator/eval/resolve.py` | 후보에 유튜브 video_id 붙이기 (길이 + 이름 겹침) |
| `Generator/eval/korean-truth.json` | **한국어 줄 단위 정답 55 곡 2,382 줄** |
| `Generator/eval/review/server.py` | 검수 서버 (바이브·LRCLIB·유튜브 찾기, 판정, 내보내기) |
| `Generator/eval/review/align.py` | demucs → wav2vec2 강제 정렬로 낱말 자리 초안 만들기 |
| `Generator/eval/review/ui/` | Apple Music 식 가사 화면 + 낱말 두드리기 |
| `packages/songtitle/src/providers/vibe.ts` | 바뀐 `syncLyric` 구조 읽도록 고침 (**제품 버그**) |
