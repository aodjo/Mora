# Admin Dashboard

React, Vite, TailwindCSS 기반 Mora Control Plane입니다. `corepack pnpm --dir Admin dev`는 UI 개발 서버를 실행하고 `/admin/api`를 로컬 Worker로 proxy합니다. production build는 Server Worker assets에 포함됩니다.
