-- 이 곡이 아닌 영상에 표를 단다.
--
-- 수집기는 제목과 길이로 후보를 모은다. 그래서 산토리에 아크라포빅 영상이 붙었고, 곡 길이마저
-- 그 영상 것(134초)으로 적혔다. 사람이 진짜 산토리(162초)를 확정해도 워커는 길이가 어긋난다며
-- 버리고 그 영상으로 넘어갔다 — 검수 화면의 타이밍이 딴 노래였던 까닭이다.
--
-- 지운다는 선택지는 없다. 옛 회차가 그 행을 가리키고 있어 외래키가 걸린다. 표를 달아 두면
-- 대체 후보에서 빠지고 화면에서도 접힌다.
ALTER TABLE media_sources ADD COLUMN rejected_at INTEGER NULL;
