-- 정렬기가 낱말마다 낸 확신을 남긴다.
--
-- 여태 후보를 넣을 때 [토큰, 시작, 끝] 만 담고 네 번째 값인 점수를 버렸다. 그래서 콘솔은
-- 어느 낱말이 실제로 재어진 것이고 어느 낱말이 자리를 못 찾아 이웃 사이에 끼워 넣은
-- 것인지 알 수 없었고, 낱말 길이로 짐작할 수밖에 없었다 — 그런데 "the" 처럼 원래 짧은
-- 낱말은 제대로 재도 바닥값과 구별되지 않아, 맞는 타이밍이 의심스럽다고 표시됐다.
--
-- word_spans 의 모양은 건드리지 않는다. 그 값은 공개 정렬로도 그대로 복사되고, 재생기가
-- 세 칸짜리로 읽는다. 점수는 검수 화면에만 필요하므로 여기에만 따로 둔다.
ALTER TABLE alignment_candidates ADD COLUMN word_scores BLOB NOT NULL DEFAULT X'5B5D';
